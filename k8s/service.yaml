apiVersion: v1
kind: Service
metadata:
  name: snake-game
  labels:
    app: snake-game
spec:
  type: ClusterIP
  selector:
    app: snake-game
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
